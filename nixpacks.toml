[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["cd backend-v2 && pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "cd backend-v2 && uvicorn app.main:app --host 0.0.0.0 --port $PORT"

